# Adds two numbers and takes the absolute value
fn add_abs(a: i32, b: f64) -> i32
{
#    let sum = add(a, b); # an inline comment
    return abs(sum);
}

fn increment(x: u8) -> u32
{
    return add(x, 1);
}
